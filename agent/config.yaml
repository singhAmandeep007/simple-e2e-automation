agent:
  id: "" # set via --id flag or env AGENT_ID
  name: "my-agent"

control_plane:
  ws_url: "ws://localhost:4000/ws"

rclone:
  # Path to the rclone binary. If blank, falls back to Go native walker.
  bin_path: "../bin/rclone"

log:
  level: "info"
